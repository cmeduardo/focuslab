import ReactionTest from '@/components/activities/ReactionTest'

export const metadata = { title: 'Reaction Test — FocusLab' }

export default function ReactionTestPage() {
  return <ReactionTest />
}
