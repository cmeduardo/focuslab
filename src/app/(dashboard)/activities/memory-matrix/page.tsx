import MemoryMatrix from '@/components/activities/MemoryMatrix'

export const metadata = { title: 'Memory Matrix — FocusLab' }

export default function MemoryMatrixPage() {
  return <MemoryMatrix />
}
